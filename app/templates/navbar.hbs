<nav class="navbar navbar-default navbar-static-top">
  <div class="navbar-header">
    {{#link-to 'home' class='navbar-brand'}} Distributed Library {{/link-to}}
  </div>

  <div class="collapse navbar-collapse">
    <ul class="nav navbar-top-links navbar-right">
      {{#if session.isLoggedIn}}
      <li><a href="#" {{action 'logout'}}>Logout</a></li>
      {{else}}
      <li><a href="#">Login</a></li>
      {{/if}}
    </ul>
  </div>

  {{#if session.isLoggedIn}}
  <div class="navbar-default sidebar">
    <div class="sidebar-nav navbar-collapse">
      <ul class="nav in">
        <li>{{#link-to 'groups'}}Groups{{/link-to}}</li>
        <li>{{#link-to 'inventory'}}My Items{{/link-to}}</li>
        <li>{{#link-to 'available-resources'}}Available Items{{/link-to}}</li>
        <li>{{#link-to 'issued-resource'}}Issued Items{{/link-to}}</li>
      </ul>
    </div>
  </div>
  {{/if}}

</nav>
